<% provide(:title, "ホーム")%>

<div class="center jumbotron">
  <% if user_signed_in? %>
  <%= render 'static_pages/home_signed_in'%>
  <% else %>
  <%= render 'static_pages/home_not_signed_in'%>
  <% end %>
</div>
