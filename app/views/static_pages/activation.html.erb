<%= provide(:title, "アカウント有効化")%>
<h1>アカウント有効化</h1>
<% if false %>
  <p class="activated">メールアドレスは有効化済みです。</p>
<% else %>
  <%= link_to "メールアドレスを検証", '#' %>
<% end %><br/>
<% if current_user.activated == true %>
  <p class="activated">電話番号は有効化済みです。</p>
<% else %>
  <%= link_to "電話番号を検証", edit_account_activation_path(current_user) %>
<% end %><br/>

<%= link_to "ホームに戻る", root_path %>
